# Insta News Studio

ニュースから今日の投稿をつくる、Instagram初心者向けの投稿素材作成Webアプリです。気になるニュースを選び、自分の短いコメントを添えると、正方形画像（1〜5枚）とキャプションを生成します。Ver.1.3は生成AIやAPIキーなしで、公開RSSの実ニュース取得、記事登録、画像編集、文章構成まで動きます。

## ユーザー向け使い方

1. 初回画面で利用目的と興味ジャンルを選ぶ
2. 「今日のおすすめ」からニュースを選ぶ
3. 自分のコメントを短く入力する
4. 投稿スタイル、文体、画像枚数、デザインを選ぶ
5. 「投稿素材を生成する」を押す
6. 画像をPNG保存し、本文をコピーする
7. 投稿ガイドを見ながらInstagramで最終投稿する
8. 投稿後にストック画面で「投稿済みにする」を押す

## Instagram初心者向け操作

Instagramで「＋」→「投稿」→保存した画像を選択→「次へ」→キャプション欄を長押し→「貼り付け」→内容確認→「シェア」の順です。最後のシェアは必ずユーザー自身が押します。投稿前に名前、数字、誤字、出典、画像の利用権限を確認してください。

## 現在の機能

- 興味ジャンル・利用目的・除外キーワード設定
- おすすめジャンルの追加・削除・名称・アイコン・説明・表示順・表示ON/OFF管理
- PCドラッグ、スマートフォン長押しドラッグ、利用回数、最近よく使うジャンルの提案
- Google News公開RSSによる実ニュース取得、端末キャッシュ、モックへの自動フォールバック
- Mock / Manual / RSSを交換可能なNewsProvider境界
- 記事URL・配信元URL・RSS URL・取得時URLの分離、URL正規化、記事ページ判定
- 「この記事を読む」「配信元を見る」「リンクをコピー」「タイトルをコピー」のニュース導線
- リンク未登録ニュースのタイトル検索と、記事本文URLを貼り付ける手動ニュース登録
- 5段階の興味度、あとで読む、興味なし
- コメント入力補助、投稿スタイル12種、文体7種
- デザインテンプレート8種、1〜5枚のカルーセル構成
- 各画像の見出し・本文・4レイアウト・順番・複製・削除の直接編集
- Canvasによる1080×1080 PNG生成・保存
- 固定ルールによる安全なキャプション、出典、ハッシュタグ生成
- 7つの文章ブロックを並べ替え、ON/OFF、加筆、整形、短縮できる文章構成エディター
- 投稿ストック、状態変更、月間カレンダー、全文検索
- PWAマニフェストとサービスワーカー
- JSONエクスポート、Instagram初心者ガイド
- localStorage保存と将来の同期先に交換できるデータ境界

## 開発環境

- vinext / Next.js 16 / React 19 / TypeScript / Tailwind CSS 4
- Cloudflare Workers互換ビルド
- Node.js 22以上、pnpm 11

```bash
pnpm install
pnpm dev
```

## 環境変数

`.env.example`を参照してください。現在のRSS取得と固定ルール生成にAPIキーは不要です。将来`OPENAI_API_KEY`などを追加する場合もサーバー側のみで使用し、`NEXT_PUBLIC_`を付けません。

## ニュース取得方法

`app/api/news/route.ts`が、表示中ジャンルと検索・除外キーワードからGoogle News RSSをサーバー側で取得します。`src/services/news/RssNewsProvider.ts`と`rss.ts`が取得・XML解析・簡易重複排除を担当し、`url.ts`が計測パラメータ除去、記事ページ判定、タイトル検索リンクを担当します。本文やニュース画像は保持せず、見出し、ルール式の短い説明、配信元、日時、リンクだけを扱います。通信失敗時は保存済みニュース、なければモックへ自動で戻ります。

## 著作権・画像利用上の注意

ニュース本文の長文転載、ニュース写真の保存・加工・再投稿をしません。投稿画像は文字、図形、ユーザーが権利を持つ写真などで構成します。ユーザー画像は、自分で撮影したか使用許可を得たものだけを利用してください。生成キャプションはニュースの概要と個人の意見を分け、配信元を明示します。

## ファイル構成

```text
app/                    画面、RSS API、UI、Canvas画像生成
src/lib/                データモデルと固定ルール生成
src/services/news/      交換可能なニュースプロバイダー
public/                 PWAマニフェスト、サービスワーカー
tests/                  自動テスト
docs/                   仕様、設計、データ、公開、引継ぎ
.github/workflows/      CI
worker/                 Cloudflare Workerエントリ
```

## テスト

```bash
pnpm typecheck
pnpm lint
pnpm test
pnpm build
```

CIでは型検査、Lint、自動テスト、ビルドを実行します。主要導線はPC幅とiPhone幅でも確認します。

## 公開方法

Sites向けのCloudflare Workers互換成果物を生成し、保存されたバージョンを公開サイトとしてデプロイします。詳細は`docs/DEPLOYMENT.md`を参照してください。

## 今後の拡張

配信元別の正式RSS追加、D1/R2同期、ユーザー写真のクロップ編集、より高度な関連記事クラスタリング、通知、縦長1080×1350を予定しています。生成AIは任意拡張とし、現在の固定ルール機能は維持します。
